---
interface Props {
  title: string;
  slug: string;
}

const { title, slug } = Astro.props;
---

<nav class="breadcrumb" aria-label="麵包屑">
  <ol>
    <li><a href="/">首頁</a></li>
    <li aria-current="page">
      <a href={`/${slug}`}>{title}</a>
    </li>
  </ol>
</nav>
